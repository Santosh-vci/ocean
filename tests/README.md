# Tests

Cross-service and fixture-level regression tests will live here once the domain workflow exists.

Chunk 0 starts with:

- backend health endpoint test;
- frontend shell render test;
- CI commands for backend and frontend lint/test execution.

